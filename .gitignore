# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Data & artifacts (large / regenerable)
data/raw/*
data/rectified/*
data/labels/*
# Repaired dataset copies from tools/fix_labels.py --out. Regenerable from the
# Roboflow export in one command, and a few hundred images is not review material.
data/mp_road/
data/*_clean/
data/rfdetr/
!data/**/.gitkeep
out/
weights/*.pt
*.pth
*.insv
*.insp
*.mp4
*.gpx
*.srt

# Models / checkpoints (auto-downloaded / trained — regenerable)
*.pt
*.onnx
*.engine

# Training scratch. Ultralytics writes here, and MLflow logs one file per
# hyperparameter — a single run adds ~150 files nobody wants in review.
runs/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
